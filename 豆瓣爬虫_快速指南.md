# 🎬 豆瓣爬虫 - 快速使用指南

我提供了 **3 个版本**的豆瓣爬虫，你可以根据自己的情况选择：

---

## 📦 三个版本对比

| 版本 | 难度 | 需要安装 | 推荐指数 |
|-----|------|---------|---------|
| **手动版** | ⭐ 简单 | `pip install requests beautifulsoup4` | ⭐⭐⭐⭐⭐ **推荐** |
| **简化版** | ⭐⭐ 一般 | `pip install selenium beautifulsoup4` + ChromeDriver | ⭐⭐⭐ |
| **完整版** | ⭐⭐⭐ 较难 | `pip install playwright` + 浏览器驱动 | ⭐⭐ |

---

## 🚀 推荐：手动版（最简单！无需 Cookie）

### 文件：`豆瓣爬虫_手动版.py`

### 第一步：安装依赖

```bash
pip install requests beautifulsoup4 lxml
```

### 第二步：配置电影（可选）

编辑 `豆瓣爬虫_手动版.py` 第 32-38 行：

```python
# 豆瓣电影讨论URL（修改为你想爬的电影）
DOUBAN_DISCUSSION_URL = "https://movie.douban.com/subject/36035676/discussion/?start=0&sort_by=time"

# 电影名称（用于保存文件）
MOVIE_NAME = "流浪地球2"

# 爬取控制
MAX_DISCUSSIONS = 50  # 改为 999999 可爬全部
MAX_REPLIES = 100     # 改为 999999 可爬全部
```

### 第三步：直接运行！

```bash
python3 豆瓣爬虫_手动版.py
```

**就这么简单！不需要 Cookie！** 🎉

### 优点
- ✅ **最简单！不需要 Cookie**
- ✅ 不需要安装浏览器驱动
- ✅ 依赖最少
- ✅ 速度快

### 如果遇到限制
- 只有在爬取失败时才需要添加 Cookie
- 脚本会提示你如何获取

---

## 💻 备选：简化版（用 Selenium）

### 文件：`豆瓣爬虫_简化版.py`

### 第一步：安装依赖

```bash
pip install selenium beautifulsoup4 lxml
```

### 第二步：安装 ChromeDriver

**Mac:**
```bash
brew install chromedriver
```

**手动安装：**
1. 访问 https://chromedriver.chromium.org/downloads
2. 下载与你的 Chrome 版本对应的 ChromeDriver
3. 解压后放到 `/usr/local/bin/` (Mac) 或系统 PATH 中

### 第三步：配置电影

编辑 `豆瓣爬虫_简化版.py` 第 20-27 行：

```python
DOUBAN_DISCUSSION_URL = "https://movie.douban.com/subject/36035676/discussion/?start=0&sort_by=time"
MOVIE_NAME = "流浪地球2"
MAX_DISCUSSIONS = 50
MAX_REPLIES = 100
```

### 第四步：运行爬虫

```bash
python3 豆瓣爬虫_简化版.py
```

**首次运行：**
- 会自动打开 Chrome 浏览器
- 如果需要登录，手动登录后按回车
- 爬虫自动开始运行

### 优点
- ✅ 自动化，无需手动获取 Cookie
- ✅ 可以看到浏览器操作过程

### 缺点
- ⚠️ 需要安装 ChromeDriver
- ⚠️ 速度较慢

---

## 📁 数据保存位置

所有版本的数据都保存在：

```
MediaCrawler/
└── data/
    └── douban/
        └── json/
            ├── 流浪地球2_讨论_2026-01-18.json
            ├── 流浪地球2_回复_2026-01-18.json
            └── 流浪地球2_统计_2026-01-18.json
```

---

## 📊 数据字段

### 讨论数据

```json
{
  "discussion_id": "讨论ID",
  "title": "讨论标题",
  "url": "讨论链接",
  "author": "发帖人",
  "reply_count": 100,
  "last_reply_time": "2023-02-15",
  "content": "初始帖子内容...",
  "images": ["图片URL1", "图片URL2"],
  "post_time": "2023-01-28 20:00",
  "replies": [...]
}
```

### 回复数据

```json
{
  "reply_index": 1,
  "author": "回复者",
  "content": "回复内容...",
  "time": "2023-01-29 09:15",
  "images": ["图片URL"],
  "discussion_id": "所属讨论ID"
}
```

---

## 🎯 五部电影的URL

```python
# 流浪地球2
"https://movie.douban.com/subject/36035676/discussion/?start=0&sort_by=time"

# 消失的她
"https://movie.douban.com/subject/35725869/discussion/?start=0&sort_by=time"

# 长安三万里
"https://movie.douban.com/subject/35943560/discussion/?start=0&sort_by=time"

# 封神第一部
"https://movie.douban.com/subject/35766477/discussion/?start=0&sort_by=time"

# 孤注一掷
"https://movie.douban.com/subject/35749412/discussion/?start=0&sort_by=time"
```

---

## ❓ 常见问题

### Q1: 需要 Cookie 吗？

**不需要！**

豆瓣的讨论页面大部分是公开的，不登录也能访问。

只有在以下情况才需要添加 Cookie：
- 爬取失败，提示需要登录
- 遇到访问频率限制

如需添加 Cookie，脚本会提示详细步骤。

### Q2: 如果遇到访问限制怎么办？

**手动版：**
- 说明 Cookie 过期或无效
- 重新获取 Cookie 并粘贴到脚本

**简化版：**
- 程序会自动打开浏览器
- 手动登录后按回车继续

### Q3: 如何爬取全部讨论？

修改配置：
```python
MAX_DISCUSSIONS = 999999  # 设置为很大的数字
MAX_REPLIES = 999999      # 设置为很大的数字
```

### Q4: ChromeDriver 版本不匹配？

**查看 Chrome 版本：**
- 打开 Chrome
- 地址栏输入 `chrome://version/`
- 查看版本号（例如：120.0.6099.109）

**下载对应版本：**
- 访问 https://chromedriver.chromium.org/downloads
- 下载对应大版本号的 ChromeDriver（例如：120）

### Q5: 数据保存在哪里？

默认保存在：
```
MediaCrawler/data/douban/json/
```

文件名格式：
```
{电影名}_讨论_{日期}.json
{电影名}_回复_{日期}.json
{电影名}_统计_{日期}.json
```

---

## 💡 推荐使用流程

### 方案一：手动版（推荐，无需 Cookie）

```bash
# 1. 安装依赖
pip install requests beautifulsoup4 lxml

# 2. 直接运行（不需要 Cookie！）
python3 豆瓣爬虫_手动版.py
```

### 方案二：简化版（如果手动版失败）

```bash
# 1. 安装依赖
pip install selenium beautifulsoup4 lxml

# 2. 安装 ChromeDriver
brew install chromedriver  # Mac

# 3. 运行
python3 豆瓣爬虫_简化版.py
```

---

## ⚠️ 重要提示

1. **学术使用** - 仅用于学术研究
2. **控制频率** - 不要爬取太快，避免被封
3. **Cookie 安全** - 不要泄露你的 Cookie
4. **定期更新** - Cookie 会过期，需要重新获取

---

## 📞 需要帮助？

如果遇到问题，请告诉我：
1. 使用的是哪个版本（手动版/简化版）
2. 具体的错误信息
3. Python 版本（`python3 --version`）

---

**祝您数据采集顺利！🎓**
