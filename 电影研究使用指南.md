# 🎬 电影观众传播影响力研究 - MediaCrawler 使用指南

> 本指南专为"中国电影观众对其传播行为影响力的意识"研究项目定制
> 研究对象：《流浪地球2》《消失的她》《长安三万里》《封神第一部》《孤注一掷》

---

## 📋 目录

1. [项目简介](#1-项目简介)
2. [环境准备](#2-环境准备)
3. [安装步骤](#3-安装步骤)
4. [快速开始](#4-快速开始)
5. [批量采集](#5-批量采集)
6. [数据说明](#6-数据说明)
7. [常见问题](#7-常见问题)
8. [注意事项](#8-注意事项)

---

## 1. 项目简介

### 已配置的平台（3个）

| 平台 | 支持状态 | 可采集数据 |
|------|---------|-----------|
| 小红书 | ✅ | 笔记内容、评论、点赞数、收藏数、发布时间、IP位置 |
| 抖音 | ✅ | 视频信息、评论、点赞数、播放量、发布时间 |
| 微博 | ✅ | 微博文本、评论、转发数、点赞数、发布时间 |

### 配置特点

- ✅ **无需数据库** - 数据保存为 JSON 格式
- ✅ **针对电影优化** - 已配置五部电影关键词
- ✅ **批量采集脚本** - 自动化采集多部电影多平台数据
- ✅ **完整评论数据** - 包含一级和二级评论
- ✅ **本地存储** - 所有数据保存在 `data/` 目录

---

## 2. 环境准备

### 系统要求

- **操作系统**: Windows / macOS / Linux
- **Python 版本**: Python 3.8 或更高
- **网络**: 稳定的网络连接
- **浏览器**: Chrome 或 Edge（最新版本）

### 检查 Python 版本

```bash
# Windows
python --version

# macOS/Linux
python3 --version
```

如果显示 `Python 3.8.x` 或更高版本，则符合要求。

### 如果没有 Python

**Windows:**
1. 访问 https://www.python.org/downloads/
2. 下载 Python 3.11 或 3.12
3. 安装时勾选 "Add Python to PATH"

**macOS:**
```bash
# 使用 Homebrew 安装
brew install python@3.11
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

---

## 3. 安装步骤

### 第一步：进入项目目录

```bash
cd MediaCrawler
```

### 第二步：安装依赖包

**Windows:**
```bash
pip install -r requirements.txt
```

**macOS/Linux:**
```bash
pip3 install -r requirements.txt
```

### 第三步：安装浏览器驱动

```bash
# Windows
python -m playwright install

# macOS/Linux
python3 -m playwright install
```

这一步会下载 Chromium 浏览器驱动（约 300MB），请耐心等待。

### 验证安装

```bash
# Windows
python main.py --help

# macOS/Linux
python3 main.py --help
```

如果显示帮助信息，说明安装成功！

---

## 4. 快速开始

### 方式一：使用配置文件（推荐）

#### 第一步：编辑配置文件

打开 `config/base_config.py`，修改以下参数：

```python
# 选择平台
PLATFORM = "xhs"  # xhs(小红书) | dy(抖音) | wb(微博)

# 选择电影（一次爬一部）
KEYWORDS = "流浪地球2"  # 或其他电影名
```

#### 第二步：运行爬虫

```bash
# Windows
python main.py --platform xhs --lt qrcode --type search

# macOS/Linux
python3 main.py --platform xhs --lt qrcode --type search
```

#### 第三步：扫码登录

1. 程序会自动打开浏览器窗口
2. 使用手机 APP 扫描二维码登录
3. 登录成功后，爬虫会自动开始采集

### 方式二：使用命令行参数

直接在命令行指定所有参数：

```bash
# 小红书 - 流浪地球2
python main.py --platform xhs --lt qrcode --type search --keywords "流浪地球2"

# 抖音 - 消失的她
python main.py --platform dy --lt qrcode --type search --keywords "消失的她"

# 微博 - 长安三万里
python main.py --platform wb --lt qrcode --type search --keywords "长安三万里"
```

---

## 5. 批量采集

### 使用批量采集脚本

项目提供了自动化批量采集工具 `电影数据采集器.py`，可以一次性采集多部电影多个平台的数据。

### 运行批量采集

```bash
# Windows
python 电影数据采集器.py

# macOS/Linux
python3 电影数据采集器.py
```

### 交互式操作流程

```
🎬 电影观众传播影响力研究 - 数据采集工具
========================================

📋 第一步：选择要采集的电影
请选择要采集的电影:
  1. 流浪地球2
  2. 消失的她
  3. 长安三万里
  4. 封神第一部
  5. 孤注一掷
  0. 全部

请输入选项编号: 0  ← 输入 0 选择全部

📋 第二步：选择要采集的平台
请选择要采集的平台:
  1. 小红书 (xhs)
  2. 抖音 (dy)
  3. 微博 (wb)
  0. 全部

请输入选项编号: 1,3  ← 输入 1,3 选择小红书和微博

确认开始采集吗？(y/n): y  ← 输入 y 确认
```

### 采集进度示例

```
========================================
📊 进度: [1/10]
🎬 当前电影: 流浪地球2
📱 当前平台: 小红书 (xhs)
========================================

🚀 执行命令: python main.py --platform xhs --lt qrcode --type search --keywords 流浪地球2

⚠️  首次运行需要扫码登录，请准备好手机！
按回车键继续...

[采集过程输出...]

✅ 采集成功！

⏳ 等待 5 秒后进行下一个任务...
```

### 采集完成总结

```
========================================
🎉 采集任务全部完成！
========================================
✅ 成功: 8 个任务
❌ 失败: 2 个任务
⏱️  总耗时: 45.3 分钟
📁 数据保存位置: ./data/ 目录
========================================
```

---

## 6. 数据说明

### 数据存储位置

所有采集的数据保存在 `data/` 目录下，按平台分类：

```
MediaCrawler/
└── data/
    ├── xhs_notes.json          # 小红书笔记数据
    ├── xhs_comments.json       # 小红书评论数据
    ├── xhs_creators.json       # 小红书创作者数据
    ├── dy_videos.json          # 抖音视频数据
    ├── dy_comments.json        # 抖音评论数据
    ├── wb_posts.json           # 微博帖子数据
    └── wb_comments.json        # 微博评论数据
```

### 小红书数据字段

#### 笔记数据 (xhs_notes.json)

```json
{
  "note_id": "笔记唯一ID",
  "type": "笔记类型 (normal/video)",
  "title": "笔记标题",
  "desc": "笔记描述内容",
  "user_id": "用户ID",
  "nickname": "用户昵称",
  "liked_count": 123,          // 点赞数
  "collected_count": 45,       // 收藏数
  "comment_count": 67,         // 评论数
  "share_count": 12,           // 分享数
  "time": "2023-02-15 10:30:00",  // 发布时间
  "ip_location": "北京",        // IP位置
  "source_keyword": "流浪地球2"  // 搜索关键词
}
```

#### 评论数据 (xhs_comments.json)

```json
{
  "comment_id": "评论唯一ID",
  "note_id": "所属笔记ID",
  "content": "评论文本内容",
  "user_id": "评论者ID",
  "nickname": "评论者昵称",
  "create_time": "2023-02-15 12:00:00",
  "like_count": 15,           // 评论点赞数
  "sub_comment_count": 3,     // 二级评论数
  "ip_location": "上海"
}
```

### 抖音数据字段

#### 视频数据 (dy_videos.json)

```json
{
  "video_id": "视频唯一ID",
  "title": "视频标题",
  "desc": "视频描述",
  "user_id": "用户ID",
  "nickname": "用户昵称",
  "liked_count": 5000,        // 点赞数
  "play_count": 50000,        // 播放量
  "comment_count": 234,       // 评论数
  "share_count": 123,         // 分享数
  "create_time": "2023-03-01 18:00:00",
  "source_keyword": "消失的她"
}
```

#### 评论数据 (dy_comments.json)

```json
{
  "comment_id": "评论ID",
  "video_id": "所属视频ID",
  "content": "评论内容",
  "user_id": "用户ID",
  "nickname": "昵称",
  "create_time": "2023-03-01 20:00:00",
  "like_count": 25
}
```

### 微博数据字段

#### 微博数据 (wb_posts.json)

```json
{
  "post_id": "微博ID",
  "content": "微博文本内容",
  "user_id": "用户ID",
  "nickname": "用户昵称",
  "like_count": 1500,         // 点赞数
  "retweet_count": 300,       // 转发数
  "comment_count": 450,       // 评论数
  "publish_time": "2023-04-01 09:00:00",
  "source_keyword": "#长安三万里#"
}
```

### 数据格式转换

如果需要将 JSON 转换为 CSV 或 Excel：

**方法1: 修改配置文件**
```python
# config/base_config.py
SAVE_DATA_OPTION = "csv"  # 或 "excel"
```

**方法2: 使用 Python 脚本转换**
```python
import json
import pandas as pd

# 读取 JSON
with open('data/xhs_notes.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

# 转换为 DataFrame
df = pd.DataFrame(data)

# 保存为 CSV
df.to_csv('xhs_notes.csv', index=False, encoding='utf-8-sig')

# 保存为 Excel
df.to_excel('xhs_notes.xlsx', index=False, engine='openpyxl')
```

---

## 7. 常见问题

### Q1: 登录二维码不显示或扫码失败

**解决方案:**
1. 关闭无头模式，让浏览器可见：
   ```python
   # config/base_config.py
   HEADLESS = False
   ```

2. 手动完成滑动验证码

3. 检查网络连接是否稳定

### Q2: 爬取数据量很少或没有历史数据

**原因:**
- 平台搜索结果通常只展示近期热门内容
- 2023年的历史数据可能无法通过关键词搜索获取

**解决方案:**
1. 如果知道具体的笔记/视频 URL，使用详情模式爬取：
   ```python
   # config/xhs_config.py
   XHS_SPECIFIED_NOTE_URL_LIST = [
       "https://www.xiaohongshu.com/explore/具体笔记ID...",
   ]
   ```

   ```bash
   python main.py --platform xhs --lt qrcode --type detail
   ```

2. 寻找话题页面或超话，爬取完整的话题讨论

3. 考虑使用其他数据源或购买历史数据

### Q3: 爬虫运行中途被封/限流

**解决方案:**
1. 增加爬取间隔：
   ```python
   # config/base_config.py
   CRAWLER_MAX_SLEEP_SEC = 5  # 增加到5秒
   ```

2. 减少并发数量（已设为1，无需调整）

3. 启用 IP 代理池（需要购买代理服务）

4. 分多次、多账号采集

### Q4: 安装 Playwright 失败

**解决方案:**
```bash
# 使用国内镜像源
pip install playwright -i https://pypi.tuna.tsinghua.edu.cn/simple

# 手动指定浏览器下载
python -m playwright install chromium
```

### Q5: 提示缺少某些 Python 包

**解决方案:**
```bash
# 重新安装所有依赖
pip install -r requirements.txt --force-reinstall

# 或单独安装缺失的包
pip install [包名]
```

### Q6: 数据文件在哪里？

**答:**
- 默认保存在 `MediaCrawler/data/` 目录
- 每次运行会追加新数据到同一个文件
- 建议定期备份数据文件

### Q7: 如何只爬某一部电影？

**方法1: 修改配置文件**
```python
# config/base_config.py
KEYWORDS = "流浪地球2"  # 只爬这一部
```

**方法2: 命令行指定**
```bash
python main.py --platform xhs --lt qrcode --type search --keywords "流浪地球2"
```

### Q8: 如何查看爬取了多少条数据？

**方法1: 查看文件大小**
```bash
# Linux/macOS
ls -lh data/

# Windows
dir data\
```

**方法2: 统计 JSON 记录数**
```python
import json

with open('data/xhs_notes.json', 'r', encoding='utf-8') as f:
    data = json.load(f)
    print(f"共采集 {len(data)} 条笔记")
```

---

## 8. 注意事项

### ⚠️ 法律与道德

1. **仅用于学术研究** - 本工具仅供学术研究使用，禁止商业用途
2. **遵守平台规则** - 遵守各平台的 robots.txt 和服务条款
3. **控制爬取频率** - 避免对平台造成过大负担
4. **保护用户隐私** - 数据脱敏，不泄露个人信息
5. **遵守法律法规** - 参考[爬虫违法违规案例](https://github.com/HiddenStrawberry/Crawler_Illegal_Cases_In_China)

### ⚙️ 技术注意事项

1. **登录状态** - 首次运行需要扫码登录，之后会自动保存登录状态
2. **数据备份** - 定期备份 `data/` 目录，避免数据丢失
3. **网络稳定** - 确保网络连接稳定，避免中途断线
4. **反爬虫** - 平台有反爬机制，合理控制爬取速度
5. **浏览器版本** - 保持 Chrome/Edge 为最新版本

### 📊 数据质量

1. **数据完整性** - 定期检查数据文件，确保没有损坏
2. **去重处理** - JSON 格式不自动去重，需手动处理
3. **时间范围** - 可能无法获取全部历史数据，以实际采集为准
4. **数据真实性** - 注意识别平台刷量、水军等虚假数据

### 🔒 安全建议

1. **不要分享登录态** - `*_user_data_dir` 目录包含登录信息，不要上传到公开仓库
2. **定期更新依赖** - 及时更新 Python 包，修复安全漏洞
3. **谨慎使用代理** - 使用可信的代理服务商

---

## 9. 进阶配置

### 修改爬取数量

```python
# config/base_config.py

# 每次爬取的笔记/视频数量（默认100）
CRAWLER_MAX_NOTES_COUNT = 200  # 增加到200

# 每个笔记/视频的评论数量（默认50）
CRAWLER_MAX_COMMENTS_COUNT_SINGLENOTES = 100  # 增加到100
```

### 启用二级评论

```python
# config/base_config.py

# 是否爬取二级评论（默认已启用）
ENABLE_GET_SUB_COMMENTS = True
```

### 修改数据保存格式

```python
# config/base_config.py

# 可选: json | csv | excel
SAVE_DATA_OPTION = "excel"  # 改为 Excel 格式
```

### 启用词云图

```python
# config/base_config.py

# 生成评论词云图
ENABLE_GET_WORDCLOUD = True
```

---

## 10. 联系与支持

### 项目地址

- GitHub: https://github.com/NanmiCoder/MediaCrawler

### 问题反馈

如遇到问题，可以：
1. 查看项目 README.md
2. 在 GitHub Issues 提问
3. 参考常见问题部分

### 数据分析建议

采集完数据后，建议使用以下工具进行分析：
- **Python**: pandas, numpy, matplotlib
- **统计分析**: SPSS, R
- **文本分析**: jieba, wordcloud, NLP 工具
- **可视化**: Tableau, Power BI

---

## 附录：完整命令参考

### 基础命令

```bash
# 查看帮助
python main.py --help

# 小红书搜索
python main.py --platform xhs --lt qrcode --type search --keywords "电影名"

# 抖音搜索
python main.py --platform dy --lt qrcode --type search --keywords "电影名"

# 微博搜索
python main.py --platform wb --lt qrcode --type search --keywords "#电影名#"
```

### 批量采集

```bash
# 运行批量采集脚本
python 电影数据采集器.py
```

### 数据转换

```bash
# 将 JSON 转换为 CSV
python -c "import pandas as pd; pd.read_json('data/xhs_notes.json').to_csv('xhs_notes.csv', index=False)"
```

---

**祝您研究顺利！🎓**
